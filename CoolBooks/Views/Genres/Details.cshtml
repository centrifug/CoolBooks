@model GenreDetailsViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>@Html.DisplayFor(model => model.Genre.Name)</h1>

<div>
    <hr />
    <dl class="row">
             <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Genre.Description)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Genre.Description)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Admin"))
    {
        <a asp-action="Edit" asp-route-id="@Model?.Genre.Id">Edit |</a>
    }
    <a asp-action="Index">Back to List</a>
</div>
<partial name="_BooksTable" model="@Model.Books" />