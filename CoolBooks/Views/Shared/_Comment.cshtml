@model List<Comment>
@{
    var routeUrl = Url.RouteUrl(ViewContext.RouteData.Values);
}
<partial name="_commentrepeat" model="@Model" />
 </ul>
 @if (signInManager.IsSignedIn(User))
        {

   <script>
    $(".like").click(function() { 
    console.log("hej");
    console.log(event.target.id);
    
    @*ajaxGet deprecated*@
    $.get("@Url.Content("~/Comments/Like")/?id=" + event.target.id + "&status=" + $(this).data("status"), function(data) {  
        var counters = data.split('/');  
        var test2 = event.target.id;
        $("#commentlikecount").text(counters[0]);  
        $("#commentdislikecount").text(counters[1]);  
        $(".like").attr('disabled', 'disabled');  
        $(".dislike").attr('disabled', false);  
        console.log(test2);
        console.log("@Url.Content("~/Comments/Like")/?id=" + test2 + "&status=" + $(this).data("status"));
        console.log("det här kanske fungerar like");
    });  
});   
$(".dislike").click(function() {  
    console.log("hej");
    console.log(event.target.id);
   
    @*ajaxGet deprecated*@
    $.get("@Url.Content("~/Comments/Like")/?id=" + event.target.id + "&status=" + $(this).data("status"), function(data) {  
        var counters = data.split('/');  
        var test = event.target.id;
        $("#commentlikecount").text(counters[0]);  
        $("#commentdislikecount").text(counters[1]);  
        $(".dislike").attr('disabled', 'disabled');  
        $(".like").attr('disabled', false);
        console.log(test);
        console.log("@Url.Content("~/Comments/Like")/?id=" + test + "&status=" + $(this).data("status"));
        console.log("det här kanske fungerar dislike");
    });  
});  
    </script>
        }